
<Window x:Class="StoreApp.Views.AddEditProductDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="بيانات المنتج"
        Width="400" Height="340"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Border Padding="24">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="الاسم *"      Style="{StaticResource LabelStyle}" Margin="0,0,0,10"/>
            <TextBox   Grid.Row="0" Grid.Column="1" Text="{Binding P_Name,            UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="SKU *"         Style="{StaticResource LabelStyle}" Margin="0,0,0,10"/>
            <TextBox   Grid.Row="1" Grid.Column="1" Text="{Binding SKU,               UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

            <TextBlock Grid.Row="2" Grid.Column="0" Text="السعر *"       Style="{StaticResource LabelStyle}" Margin="0,0,0,10"/>
            <TextBox   Grid.Row="2" Grid.Column="1" Text="{Binding Price,             UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

            <TextBlock Grid.Row="3" Grid.Column="0" Text="الكمية *"      Style="{StaticResource LabelStyle}" Margin="0,0,0,10"/>
            <TextBox   Grid.Row="3" Grid.Column="1" Text="{Binding StockQuantity,     UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

            <TextBlock Grid.Row="4" Grid.Column="0" Text="حد التنبيه"   Style="{StaticResource LabelStyle}" Margin="0,0,0,10"/>
            <TextBox   Grid.Row="4" Grid.Column="1" Text="{Binding LowStockThreshold, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

            <StackPanel Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2"
                        Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="إلغاء" IsCancel="True" Width="90" Height="34" Margin="0,0,10,0"/>
                <Button Content="حفظ ✓" IsDefault="True" Width="90" Height="34"
                        Style="{StaticResource SuccessButton}"
                        Click="SaveButton_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
